//C#
using UnityEngine;
using System.Collections;

public class col : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnCollisionEnter(Collision collision){
		//renderer.material.color=Color.blue
			//Destroy(gameObject);

		Application.LoadLevel("inicio");
	}
}

//JavaScript
//#pragma strict
var enemigo : Transform;


function OnCollisionEnter(enemigo : Collision)
{
if(enemigo.gameObject.name=="enemigo"){
Debug.Log("holas");

}}
/*if(enemy_col.gameObject.name=="Terrain"){
Debug.Log("holas");

}
	//Application.LoadLevel("OPCIONES");
	
	
}

/*function OnTriggerEnter (other : Collider)
{
	Debug.Log("Colision con Fantasma!"); //<----------------------------------------------------------
	
}*/

//C# Colision con Esfera
using UnityEngine;
using System.Collections;

public class collevel1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	void OnCollisionEnter(Collision collision){
		//renderer.material.color=Color.blue
		//Destroy(gameObject);
		
		Application.LoadLevel("scenalaberinto");
	}
}

